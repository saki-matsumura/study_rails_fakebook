<!DOCTYPE html>
<html>
  <head>
    <title>Fakebook</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/css/bootstrap.min.css" integrity="sha384-9aIt2nRpC12Uk9gS9baDl411NQApFmC26EwAOH8WgZl5MYYxFfc+NcPb1dKGj7Sk" crossorigin="anonymous">

    <%= stylesheet_link_tag "application", media: "all" %>
    <%= javascript_pack_tag "application" %>
  </head>

  <body>
    <% flash.each do |key, value| %>
      <%= content_tag(:div, value, class: "#{key}") %>
    <% end %>
    <header class = "sticky-top">
      <div class="container">
        <div class = "row">
          <div class = "app_name text-left col-sm mt-3">
            <h2><%= link_to "Fakebook", blogs_path %></h2>
          </div>
          <div class = "login text-right  col-sm mt-3">
          <% if logged_in? %>
            <%# ログイン中 %>
            <%= link_to "Plofile", user_path(current_user.id), class: "btn btn-light" %>
            <%= link_to "Logout", session_path(current_user.id), method: :delete, class: "btn btn-light"  %>
          <%else%>
            <%# ログアウト中 %>
            <%= link_to "Sign up", new_user_path, class: "btn btn-light" %>
            <%= link_to "Login", new_session_path, class: "btn btn-light" %>
          <% end %>
          </div>
        </div>
      </div>
    </header>
        <%= yield %>
  </body>
</html>
